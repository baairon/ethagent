node_modules
dist/
*.log
.env
.env.local

# Root continuity markdown can contain private local identity data.
# Keep this scoped to the repository root so nested docs remain trackable.
/SOUL.md
/MEMORY.md
/skills.json
